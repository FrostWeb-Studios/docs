name: Security

on:
  push:
    branches: [master]
  pull_request:
    branches: [master]
  schedule:
    - cron: "27 4 * * 1"

permissions:
  contents: read

# CodeQL omitted: CodeQL has no first-party support for this repo's language.
# Gitleaks + dependency review still apply.
jobs:
  secret-scan:
    permissions:
      contents: read
    uses: FrostWeb-Studios/.github/.github/workflows/reusable-gitleaks.yml@a703743f414d3c6a0137bf6c689475d2f9b10606 # v1

  dependency-review:
    if: github.event_name == 'pull_request'
    permissions:
      contents: read
      pull-requests: write
    uses: FrostWeb-Studios/.github/.github/workflows/reusable-dependency-review.yml@a703743f414d3c6a0137bf6c689475d2f9b10606 # v1
    with:
      fail-on-severity: high
